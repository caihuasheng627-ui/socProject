# SkinVision AI 后端依赖(组员 3)
# 安装: pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/

# Web 框架
fastapi>=0.110
uvicorn[standard]>=0.29

# LLM / HTTP
httpx>=0.27

# 定时任务 / RSS
APScheduler>=3.10
feedparser>=6.0

# 配置
python-dotenv>=1.0

# 认证(JWT + bcrypt 哈希;直接用 bcrypt 包,不依赖 passlib 以避 5.x 兼容问题)
PyJWT>=2.8
bcrypt>=4.0

# 数据处理(已随 Anaconda)
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3

# 深度学习(LSTM-C/D/GRU 推理;组员 1 训练用)
tensorflow>=2.15
lightgbm>=4.0
# xgboost 可选(缺失时 XGBoost 端点走 preds/pred_xgboost.csv 兜底)
xgboost>=2.0
